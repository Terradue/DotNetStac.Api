namespace Stac.Api.Interfaces
{
    public enum GeometryOperation
    {
        Intersects,
        Contains,
        Disjoint,
        Equals,
        Overlaps,
        Touches,
        Within
    }
}